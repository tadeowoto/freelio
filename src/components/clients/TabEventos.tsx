export default function TabEventos({ clientId }: { clientId: string }) {
  return (
    <div className="text-center py-16 text-steel-gray font-body">
      Los eventos de este cliente aparecerán acá.
    </div>
  );
}
